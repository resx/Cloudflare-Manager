# API 基础路径
VITE_API_BASE_URL=/api

# 注意：Cloudflare API 凭证应该由前端用户在界面中输入
# 不要将敏感凭证硬编码在前端代码或环境变量中

# 推荐使用 API Token（在请求中传递）
# 格式: { "credentials": { "api_token": "user_token" }, ... }

# 旧式认证方式（不推荐，但支持）
# 格式: { "credentials": { "email": "user@example.com", "api_key": "key" }, ... }
