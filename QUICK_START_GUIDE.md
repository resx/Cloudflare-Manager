# 新功能快速指南

本文档介绍最新实现的功能及使用方法。

---

## 🎯 新增功能概览

### 1. 缓存管理增强 ✨

**位置**: 缓存配置页面

**新功能**:
- 🗑️ 清除所有缓存
- 📄 按URL清除缓存（最多30个）
- 🏷️ 按标签清除缓存（最多30个）
- 📊 实时缓存统计（从Analytics获取）

**使用方法**:
```
1. 进入"缓存配置"页面
2. 在"清除缓存"卡片中选择清除方式
3. 根据选择的方式输入URL或标签
4. 点击"清除"按钮并确认
```

**注意事项**:
- 清除所有缓存会影响全站，请谨慎操作
- URL需要完整路径（包含协议）
- 标签清除需要预先在资源响应头中设置Cache-Tag

---

### 2. SSL证书信息展示 🔒

**位置**: SSL/TLS 设置页面

**新功能**:
- 自动获取SSL证书信息
- 显示证书状态、类型、颁发者、签名算法

**数据说明**:
- **证书状态**: 有效/未知
- **证书类型**: Universal SSL / Custom SSL等
- **颁发者**: Let's Encrypt / DigiCert等
- **签名算法**: SHA256-RSA / ECDSA等

**刷新方式**: 切换域名或刷新页面自动更新

---

### 3. 页面规则管理 📋

**位置**: 页面规则页面

**功能说明**:
页面规则允许您为特定URL模式自定义Cloudflare设置。

**支持的操作**:
- 缓存级别（5种选项）
- 浏览器缓存TTL（8种时长）
- SSL模式（4种模式）
- 安全级别（5种级别）

**使用流程**:
```
1. 点击"创建规则"按钮
2. 输入URL模式（支持通配符*）
   示例: example.com/blog/*
3. 选择要应用的操作和值
4. 设置规则状态（启用/禁用）
5. 保存规则
```

**URL模式示例**:
```
example.com/*             # 匹配整个域名
example.com/blog/*        # 匹配blog目录下所有页面
*.example.com/*           # 匹配所有子域名
example.com/api/*.json    # 匹配特定文件类型
```

**限制说明**:
- 免费计划最多3条规则
- 规则按优先级执行（1最高）
- 某些操作需要付费计划

---

### 4. DNS批量导入 📦

**位置**: DNS记录管理页面

**新功能**:
一次性导入多条DNS记录，大幅提升配置效率。

**支持方式**:
1. **粘贴文本**: 直接粘贴CSV格式数据
2. **上传文件**: 上传.csv或.txt文件

**CSV格式说明**:
```csv
类型,名称,内容,TTL,是否代理,优先级
A,www,192.168.1.1,3600,true
AAAA,www,2001:db8::1,3600,true
CNAME,blog,example.com,1,false
MX,@,mail.example.com,3600,false,10
TXT,@,"v=spf1 include:_spf.example.com ~all",1,false
```

**字段说明**:
- **类型**: A, AAAA, CNAME, MX, TXT, SRV, NS, CAA, PTR
- **名称**: @ (根域名), www, blog 等
- **内容**: IP地址、域名、文本等
- **TTL**: 1=自动, 其他值为秒数
- **是否代理**: true/false
- **优先级**: 仅MX和SRV记录需要

**使用步骤**:
```
1. 点击"批量导入"按钮
2. 选择导入方式（粘贴/上传）
3. 输入或上传CSV数据
4. 点击"解析"按钮
5. 检查预览结果是否正确
6. 点击"导入 X 条记录"确认
7. 等待导入完成
```

**注意事项**:
- 每行一条记录
- 井号(#)开头的行会被忽略（可用作注释）
- 空行会被自动跳过
- 导入失败的记录不影响其他记录
- TXT记录内容需要用引号包裹

**示例文件**:
```csv
# 基础A记录
A,@,192.168.1.1,3600,true
A,www,192.168.1.1,3600,true

# IPv6记录
AAAA,@,2001:db8::1,3600,true

# CNAME记录
CNAME,blog,example.com,1,false
CNAME,shop,shopify.com,1,false

# 邮件记录
MX,@,mail.example.com,3600,false,10
MX,@,mail2.example.com,3600,false,20

# SPF记录
TXT,@,"v=spf1 include:_spf.google.com ~all",1,false
```

---

## 💡 使用技巧

### 缓存优化建议
1. 静态资源（CSS/JS/图片）使用"缓存一切"
2. API响应使用较短的缓存时间
3. 定期清除缓存确保内容更新

### 页面规则最佳实践
1. 优先级从高到低排列（1最高）
2. 更具体的规则优先级更高
3. 避免规则冲突

### DNS批量导入技巧
1. 先在Excel/Google Sheets准备数据
2. 导出为CSV格式
3. 使用"解析"功能预览结果
4. 小批量测试后再大批量导入

---

## ⚠️ 常见问题

### Q: 清除缓存后需要多久生效？
A: 通常在几秒到几分钟内生效，全球节点可能有延迟。

### Q: 为什么SSL证书信息显示为空？
A: 可能原因：
- 域名尚未启用SSL
- Cloudflare正在签发证书（需要等待）
- API权限不足

### Q: 页面规则为什么不生效？
A: 检查项目：
- 规则状态是否为"已启用"
- URL模式是否正确匹配
- 规则优先级是否被覆盖
- 是否有DNS配置问题

### Q: 批量导入失败怎么办？
A: 解决方案：
1. 检查CSV格式是否正确
2. 查看错误提示信息
3. 确认API Token权限
4. 分批次导入减少失败率

---

## 🔧 故障排除

### 缓存清除失败
- 检查网络连接
- 确认API凭证有效
- 验证URL格式正确

### SSL证书无法获取
- 确认域名已添加到Cloudflare
- 检查DNS记录是否正确
- 等待证书签发完成（可能需要24小时）

### 页面规则保存失败
- 检查是否超过免费计划限制（3条）
- 验证URL模式语法
- 确认操作值合法

### DNS批量导入错误
- 逐行检查CSV格式
- 使用"解析"功能查看详细错误
- 确认记录内容符合DNS规范

---

## 📞 获取帮助

- 查看详细文档: `FEATURE_IMPLEMENTATION_SUMMARY.md`
- 查看部署指南: `DEPLOYMENT.md`
- 查看项目概览: `PROJECT_OVERVIEW.md`

---

**最后更新**: 2025-11-06
**版本**: 1.0
